name: Build OpenClaw iOS IPA

on:
  workflow_dispatch:

jobs:
  build:
    name: Build and Package
    runs-on: macos-latest

    steps:
      - name: Checkout Source Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install pnpm
        run: npm install -g pnpm

      - name: Install xcodegen
        run: brew install xcodegen

      - name: Install Dependencies
        run: pnpm install
        
      - name: Install Dependencies
        run: pnpm install

      - name: Hack Swift Tools Version
        # 在整个项目中全局搜索 Package.swift，并把开头的 6.2 强行替换成 6.1
        run: |
          echo "开始降级 Swift Tools 版本..."
          find . -name "Package.swift" -exec sed -i '' 's/swift-tools-version: 6.2.0/swift-tools-version: 6.1.0/g' {} +
          find . -name "Package.swift" -exec sed -i '' 's/swift-tools-version: 6.2/swift-tools-version: 6.1/g' {} +
          find . -name "Package.swift" -exec sed -i '' 's/swift-tools-version:6.2/swift-tools-version:6.1/g' {} +
          echo "✅ 版本降级替换完成！"

      - name: Generate Xcode Project
        run: pnpm ios:gen

      - name: Build Unsigned iOS App
        # 【关键修复】明确告诉服务器，进入 apps/ios 目录执行编译！
        working-directory: apps/ios
        run: |
          PROJECT_PATH=$(find . -name "*.xcodeproj" | head -n 1)
          echo "🎉 找到工程文件: $PROJECT_PATH"
          
          PROJECT_NAME=$(basename "$PROJECT_PATH" .xcodeproj)
          echo "🚀 使用 Scheme: $PROJECT_NAME"
          
          xcodebuild clean build \
            -project "$PROJECT_PATH" \
            -scheme "$PROJECT_NAME" \
            -configuration Release \
            -sdk iphoneos \
            CONFIGURATION_BUILD_DIR="${PWD}/build" \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO

      - name: Package into IPA
        # 【关键修复】同样在 apps/ios 目录下进行打包
        working-directory: apps/ios
        run: |
          mkdir -p Payload
          cp -R build/*.app Payload/
          zip -qr OpenClaw.ipa Payload

      - name: Upload IPA Artifact
        uses: actions/upload-artifact@v4
        with:
          name: OpenClaw-iOS-App
          # 【关键修复】从正确的子目录把打包好的文件提取出来上传
          path: apps/ios/OpenClaw.ipa
